query getRecyclingGuideSearchEngine($searchTerm: String!, $contractId: ID!) {
  recyclingGuideSearchEngine(searchTerm: $searchTerm, contractId: $contractId) {
    typeName
    id
    name
    wasteFamilyName
  }
}
