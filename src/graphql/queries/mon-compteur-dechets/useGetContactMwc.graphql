query getContact($filters: MwCounterServiceFiltersInput) {
  mwCounterServices(filters: $filters) {
    data {
      attributes {
        serviceName
        contactEmail
        phoneNumber
        postalAddress
        postalCode
        city
      }
    }
  }
}
