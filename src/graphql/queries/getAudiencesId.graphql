query getAudiencesId($contractId: ID) {
  contract(id: $contractId) {
    data {
      id
      attributes {
        audiences {
          data {
            id
          }
        }
      }
    }
  }
}
