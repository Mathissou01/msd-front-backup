query getNewestTopContents($contractId: ID!) {
  getNewestTopContents(contractId: $contractId) {
    type
    originalId
    title
    shortDescription
    publishedDate
    tags {
      name
    }
    image {
      hash
      mime
      name
      provider
      size
      url
      alternativeText
    }
  }
}
