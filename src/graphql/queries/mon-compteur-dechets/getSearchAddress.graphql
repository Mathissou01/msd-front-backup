query SearchAddress($address: String, $limit: Int) {
  searchAddress(address: $address, limit: $limit) {
    label
    score
    id
    name
    postcode
    citycode
    x
    y
    city
    district
    context
    type
    importance
    street
    houseNumber
  }
}
