query getFooterBlock($contractId: ID!) {
  contractCustomizations(filters: { contract: { id: { eq: $contractId } } }) {
    data {
      attributes {
        footer {
          data {
            id
            attributes {
              accessibilityLevel
              legalContent {
                data {
                  attributes {
                    GCULink
                    accessibilityLink
                    confidentiality
                    cookiesPolicy
                  }
                }
              }
            }
          }
        }
      }
    }
  }
  contactUsServices(filters: { contract: { id: { eq: $contractId } } }) {
    data {
      attributes {
        isActivated
        label
        link
      }
    }
  }
}
