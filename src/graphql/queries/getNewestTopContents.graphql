query getNewestTopContents($contractId: ID!) {
  getNewestTopContents(contractId: $contractId) {
    originalId
    title
    shortDescription
    publishedAt
    tags {
      name
    }
    image {
      hash
      mime
      name
      provider
      size
      url
      alternativeText
    }
  }
}
